# Next Steps

- **Phase 3 — Scale Collection**:
  - Ingest new cosmetic topics (AHA/BHA, Vitamin C, peptides, physical devices) safely using the hardened write gate (`verify_gate.py`).
  - Keep graph size expanding while preventing low-quality, unsupported facts.
- **Phase 3.5 — Full Design System & Multi-Renderer Output**:
  - Implement the remaining 4 layout styles in the HTML render engine (to cover the complete 10 layout system).
  - Implement python-pptx as a secondary rendering engine to compile editable PowerPoint presentations.

