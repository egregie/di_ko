# YM PROSKIN — Phase 4.2 (Blocks A+B): Real Exa Pilot + Honest Backfill

Версия: 1.1 · Дата: 2026-06-07 · Блок 0 этого ТЗ УЖЕ ВЫПОЛНЕН (DNS-движок починен,
MCP-конфиг очищен, Exa проведена). Здесь — только остаток: Блоки A и B.

> Свежей сессии агента: СНАЧАЛА прочитай `00_PROJECT_STATE/current_state.md`,
> `next_steps.md`, `decisions_log.md`, `known_problems.md` и `00_governance/AGENTS.md`
> (принципы P001–P015) — это твоя рабочая память. Только потом исполняй.

Состояние графа (после Phase 4.1/4.2 Block 0): 29 активных фактов, движок верификации живой
(системный DNS + IPv4 + NCBI tool/email/UA + rate-limit). Тонкие темы НИЖЕ P011:
**Vitamin C: 5, Niacinamide: 7, Exfoliants: 5** (порог — 8 чистых фактов).

Легенда: `[GUARD]` жёсткое ограничение · `[ВЕРИФ]` · `[НЕТ ДАННЫХ]`.

---

## Жёсткие GUARD (нарушались ранее — соблюдать строго)
- Только живая верификация (`verify_gate --force-live` на выборке); **засев кэша/вердиктов
  запрещён** (P009/P014).
- Нет доступа к зависимости (Exa/eutils) → `status: blocked` + диагноз, **не симуляция/мок-PASS**
  (P012/P013).
- Сетевые обходы (DoH/прокси) — только с согласия пользователя + DEC (P015).
- **Не добивать факты ради порога** (P007): мало реальных Tier-A/B → тема остаётся non-deck-ready.
- `walkthrough.md` строго по шаблону; `status` честный.

---

## Блок A — Настоящий Exa-пилот
Exa теперь реально подключена (project-grant + `EXA_API_KEY`). Прогнать честный A/B.
1. Тема пилота: **Vitamin C** (она же из тонких — факты пойдут в зачёт Блока B).
2. Два пути сбора кандидатов: (1) стандартный поиск, (2) Exa-grounded. Оба кандидата-факта
   пропустить через живой `verify_gate --force-live`.
   `[GUARD]` Если Exa недоступна (ключ/MCP/сеть) — `status: blocked`, диагноз, без мока (урок
   симулированного пилота из Phase 4).
3. Зафиксировать ЧЕСТНУЮ rejection-rate по каждому пути → `ops/logs/exa_pilot_real.md`
   (заменяет/дополняет симулированный `exa_pilot.md`, помеченный как deprecated).
4. Обновить `DEC-012` реальными числами: adopt Exa / defer — по факту (брак vs baseline 27%,
   доля Tier-A в выдаче, расход кредитов $10).

Acceptance: `exa_pilot_real.md` с живыми числами обоих путей; `DEC-012` отражает реальный итог.

---

## Блок B — Честный добор тонких тем до P011 (≥8 чистых фактов)
Цели (минимум до порога): **Vitamin C 5→≥8, Niacinamide 7→≥8, Exfoliants 5→≥8.**
1. Сбор доп. фактов выигравшим путём из Блока A → живой `verify_gate` → в граф только
   `SUPPORTED`. Факты Vit C из пилота засчитываются здесь.
2. `[GUARD]` Не добивать мусором: если по теме честно нет ≥8 Tier-A/B — оставить non-deck-ready,
   причину в `known_problems.md` (P007/P011). Ненулевой брак — норма.
3. Обновить deck-readiness честно в `current_state.md`/`known_problems.md`.

Acceptance: каждая тема ≥8 чистых ИЛИ задокументированный предел; реальные числа в
`ops/logs/scale_quality.md`.

---

## Блок C — Онтология и целостность
1. A07 глобально после добавлений: 0 межтемных дублей; aliases слиты (ascorbic acid/vit C;
   nicotinamide/vit B3; деривативы C → родитель Vitamin C); кросс-связи только с источником.
2. Пересобрать индекс (`build_index.py`); `validate_graph.py` 0 ошибок; provenance-integrity (P014) держится.
3. `retriever.py` по каждой добитой теме + рабочий кросс-темный запрос.

---

## Definition of Done
- [ ] Exa-пилот выполнен живьём; `exa_pilot_real.md`; `DEC-012` с реальными числами (adopt/defer).
- [ ] Тонкие темы ≥8 чистых ИЛИ предел задокументирован; deck-readiness честно обновлён.
- [ ] A07 чисто (0 дублей); индекс пересобран; `validate_graph` 0 ошибок; provenance держится.
- [ ] retriever по темам + кросс-темный запрос работают.
- [ ] `walkthrough.md` по шаблону, честный `status`; коммит + тег `phase-4.2-exa-backfill`.

---

## НЕ трогать
- Рендер/дизайн (готовы). Новые темы (Peptides) — только после того как 3 тонкие добиты честно.
- `ontology_v1.json` — изменения только через A07.

## Открыто `[ВЕРИФ] / [НЕТ ДАННЫХ]`
- Поднялась ли Exa без «MCP Error» после перезапуска IDE. `[ВЕРИФ]`
- Реальная глубина Tier-A по тонким темам (хватит ли на ≥8). `[НЕТ ДАННЫХ]` — выяснится при сборе.

---

### Выход
`walkthrough.md` по шаблону. В snapshot: реальные числа Exa-пилота обоих путей (vs 27%),
итог `DEC-012`, факты на тему после добора, какие темы стали deck-ready (≥8), статус A07 (дубли: 0?).
